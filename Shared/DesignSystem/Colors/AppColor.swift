//
//  AppColor.swift
//  Hira
//
//  Created by Ryuk on 02/04/26.
//

import SwiftUI

enum AppColor {

    // MARK: Background
    static let background = Color("BackgroundColor")
    static let foreground = Color("ForegroundColor")

    // MARK: Card
    static let card = Color("CardColor")
    static let cardForeground = Color("CardForegroundColor")

    // MARK: Popover
    static let popover = Color("PopoverColor")
    static let popoverForeground = Color("PopoverForegroundColor")

    // MARK: Primary
    static let primary = Color("AppPrimaryColor")
    static let primaryForeground = Color("PrimaryForegroundColor")

    // MARK: Secondary
    static let secondary = Color("AppSecondaryColor")
    static let secondaryForeground = Color("SecondaryForegroundColor")

    // MARK: Muted
    static let muted = Color("MutedColor")
    static let mutedForeground = Color("MutedForegroundColor")

    // MARK: Accent
    static let accent = Color("AccentColor")
    static let accentForeground = Color("AccentForegroundColor")

    // MARK: Destructive
    static let destructive = Color("DestructiveColor")
    static let destructiveForeground = Color("DestructiveForegroundColor")

    // MARK: Borders
    static let border = Color("BorderColor")
    static let input = Color("InputColor")
    static let ring = Color("RingColor")

    // MARK: Quran Special (tambahan penting)
    static let ayahText = Color("AyahTextColor")
    static let ayahHighlight = Color("AyahHighlightColor")
    static let playbackActive = Color("PlaybackActiveColor")
    static let downloadProgress = Color("DownloadProgressColor")
}
